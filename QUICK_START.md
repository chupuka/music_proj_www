# Быстрый старт (5 минут)

## Для нового пользователя проекта

### 1. Клонирование и установка зависимостей

```bash
git clone https://github.com/YOUR_USERNAME/music-streaming-platform.git
cd music-streaming-platform
```

### 2. Настройка базы данных через Docker (самый простой способ!)

**Windows (PowerShell):**
```powershell
.\start-db.ps1
```

**Linux/Mac:**
```bash
docker-compose up -d
```

База данных создастся автоматически со всеми настройками! Больше ничего делать не нужно. ✅

> **Если Docker не установлен:** Скачайте Docker Desktop с https://www.docker.com/products/docker-desktop

> **Альтернатива:** Если хотите использовать локальную PostgreSQL, см. `DATABASE_SETUP.md`

### 3. Запуск сервисов

**Windows:**
```powershell
.\start-all.ps1
```

**Linux/Mac:** Откройте 6 терминалов и запустите:
```bash
# 1. Config Server
cd config-server && mvn spring-boot:run

# 2. Eureka Server
cd eureka-server && mvn spring-boot:run

# 3-6. Остальные сервисы (см. DEPLOYMENT.md)
```

### 4. Запуск Frontend

```bash
cd frontend
python -m http.server 3000
```

### 5. Готово!

Откройте: http://localhost:3000

## Проверка работы

- ✅ Eureka Dashboard: http://localhost:8761 (все сервисы должны быть видны)
- ✅ Frontend: http://localhost:3000
- ✅ API Gateway: http://localhost:8080

## Что дальше?

- Создайте пользователя через регистрацию
- Добавьте треки через админ-панель (🔒 в шапке)
- Наслаждайтесь музыкой!


